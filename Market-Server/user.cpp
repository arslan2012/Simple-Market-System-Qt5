#include "user.h"/*****          User           *****/int User::count = 0;User::User(QString _userName, QString _password, double _balance) {    log = false;    id = count++;    userName = _userName;    password = _password;    balance = _balance;}bool User::login(QString _userName, QString _password) {    if (_userName == userName && _password == password) {        log = true;        return true;    } else {        return false;    }}void User::logout() {    log = false;}QString User::getInformation() {    return "User ID:"+QString::number(id)+" UserName:"+userName+QString(" Balance: %1").arg(balance);}QStringList User::getGoodsList(std::vector<Goods *> goods){    QStringList list;    for (size_t i = 0; i < goods.size(); i++)    {        list.append(goods[i]->getMessage());    }    return list;}QStringList User::getGoodsList(std::vector<Goods *> goods,QString str){    QStringList list;    for (size_t i = 0; i < goods.size(); i++)    {   if (str == "" || goods[i]->getName().contains(str))        {            list.append(goods[i]->getMessage());        }    }    return list;}